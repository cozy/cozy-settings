@require 'cozy-ui'
@require './spinner.styl'

.set-loading
  text-align center
  &:before
    content     ''
    margin      em(80px) auto 0
    width       em(80px)
    height      em(80px)
    @extend $icon-spinner-blue

  p
    margin-top  em(15px)
    color       grey-11
    line-height 1.5
